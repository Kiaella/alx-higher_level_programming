#!/usr/bin/python3
# Author - Odoemenam El

def islower(c):
    """Function checks for lowercase characters."""
    if ord(c) >= 97 and ord(C) <= 122:
        return True
    else:
        return False
